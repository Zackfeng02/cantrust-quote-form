# 本轮验证 · 2026-09-10

## 自动化

- `npm test`：51 passed，0 failed。其中覆盖图片从 AI 请求中隔离、纯图片不得触发 AI 整理、客户关联创建/清除审计、演示团队拒绝客户查询、管理员密码恢复一次性消费与旧会话撤销，以及 ClientCore HMAC 请求和最小字段解析。
- `npm run typecheck`：通过。
- `npm run build`：Next.js 16.3.4 生产构建通过，包含客户候选查询 `/api/customers` 在内的网页与 API 路由。
- ClientCore：`npm exec -- vitest --run backend/team-kanban-integration.test.js` 为 2 passed，`npm run lint` 通过；专用查询接口只返回客户 ID、编码、显示名和匹配方式。
- 运行时 HMAC 探针：使用随机、不会命中真实客户的查询调用本机 ClientCore，得到 HTTP 200 和空候选列表；未输出密钥或客户资料。
- `node --env-file=.env.local scripts/http-smoke.ts`：8 项通过。使用独立的虚构验收团队，结果在忽略文件 `evidence/http-smoke-report.json`。

HTTP 验证覆盖匿名拒绝、私人来源附件隔离、真实 GIF 字节获取及签名篡改、并发 200/409、显式追加后成员可读、邀请一次性使用、移除后会话及附件立即拒绝、跨站修改拒绝。

## 浏览器

在本机真实 Next.js 服务中完成：

- 进入独立虚构演示空间，显示桌面四列看板。
- 新建“浏览器验收：跟进测试客户”，填写虚构客户、负责人和两个检查项。
- 修改为等待外部，填写原因并完成一个检查项，保存后卡片显示 1/2 和等待原因。
- 在手机尺寸检查状态标签、底部导航与资料收件箱；文档宽度与可视宽度一致，没有横向溢出。
- 修复纯日期按 UTC 解析造成的少显示一天问题，最终构建重新加载验证。

浏览器尺寸检查不替代 iOS/Android 企业微信和微信真机验收。部分内置浏览器截图在放大视口时存在未绘制区域，桌面结构和表单交互以 DOM/行为核实；不把该截图当成完整桌面视觉证据。

## 运行状态

本地网页 http://localhost:3000 与 ClientCore 专用查询服务 http://127.0.0.1:5174/health 均已返回 200。本轮没有把收件箱中的资料送入 AI，也不依赖接入 worker 运行来完成客户候选查询。

## 未验证与上线条件

- DeepSeek V4.1 Flash 已按官方公告改用 `deepseek-flash`；`npm run ai:verify` 已通过模型列表实时验证。适配层使用模拟响应测试通过，不等于真实聊天资料已发送或整理成功；图片不会送入该适配层。
- 此版本绑定账号后“真机转发→收件箱→AI→确认建卡”的完整新链路尚未实测。
- 之前单聊接收测试只证明文字和图片可收到，不证明合并记录可展开。
- PostgreSQL 服务及 S3 分支已实现配置，当前实际验证的是本地 PGlite 和私有文件分支；生产服务、部署地区、保留期限、备份恢复仍待确认和实测。
- 密码找回采用本机管理员一次性恢复码，不依赖邮箱；邮箱验证码尚未配置。
- 单聊消息接收先持久化后同步，可恢复已落盘资料；不保证平台重放离线期间未收到的消息。

最终补充：邀请链接可从已有演示登录切换到注册页，浏览器已验证“加入团队”表单出现；手机“退出”入口已验证可见。最终生产构建通过，网页和机器人 worker 已重新启动，认证成功。首次管理员注册页面已在 Codex 预览中打开，尚未代用户创建个人账号。
